{
  "name": "@axonsdk/monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "AxonSDK TypeScript monorepo (workspace root, not published). Published packages live under packages/*.",
  "workspaces": [
    "packages/*",
    "templates/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build --filter=./packages/*",
    "dev": "turbo run dev",
    "test": "turbo run test --filter=./packages/*",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.10.4",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20.x"
  }
}
